import React from 'react';

const Login=()=>{
return <div>
    email:<textarea name="email" > </textarea>
    <br/>
    password:<textarea name="email" > </textarea>
    <button>enter</button>
</div>
}
export default Login;
